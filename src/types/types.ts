export type ProductTypes = {
    id: number;
    title: string;
    description: string;
    price: number;
    thumbnail: {url: string}
    category: string;
    createdAt: string;
    updatedAt: string; 
}